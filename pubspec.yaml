name: brand_theming
description: Sistema de temas multi-marca para Flutter com tokens tipados, motion acessivel, skeletons e texturas deterministicas.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
